# Pangea — project context for Claude Code

Mobile-first geography learning game (Duolingo-inspired, original brand).
Live at **https://magnus706.github.io/atlas-sprint/** · installable PWA.
Owner: Magnus (magnus706). Norwegian; non-US founder.

## Working on this repo

- **Stack**: Next.js 14 (App Router, `output: "export"` static), TypeScript strict,
  Tailwind, Framer Motion. No backend yet — all state in localStorage.
- **Dev**: `npm run dev` → http://localhost:3210. Typecheck with `npx tsc --noEmit`.
- **NEVER run `npm run build` while the dev server is running** — both write to
  `.next/` and the dev server starts serving broken chunks (blank page). Stop dev,
  `rm -rf .next`, build, then restart dev fresh.
- **Deploy**: push to `main` → GitHub Actions builds and deploys to Pages
  automatically (~90s). Nothing else needed. The Pages build sets
  `NEXT_PUBLIC_BASE_PATH=/atlas-sprint`; local dev serves at root. Any new asset
  or manifest link must respect the base path (see layout.tsx / InstallPrompt).
- localStorage key is `atlas-sprint-v1` — do not rename (wipes user progress).
  App display name is Pangea; repo/URL still "atlas-sprint" (rename = optional
  future task: rename GitHub repo + update basePath in workflow + next.config).

## Brand

- Name **Pangea**, mascot **Pan** (teal dragon-ish creature with a map-pin tail;
  art in `public/mascot/*.png`, sources in `mascot-src/`, processed by
  `scripts/process-mascot.mjs` — flood-fill background keying, measured tolerance).
- Design: white cards on soft teal canvas (#EDF8F6), hairline borders (#E5E5E5),
  signature teal #00B2A9, flat accents (blue/purple/orange/red/yellow), Nunito
  800/900, thick pressed 3D buttons with UPPERCASE labels, no emoji anywhere —
  custom SVG icons in `src/components/icons.tsx`.
- Icons/manifest generated by `scripts/gen-icons.mjs` (sharp) from mascot art.

## Architecture map

```
src/data/countries.ts   146 countries: capital, ISO codes, pop, area, borders
src/data/facts.ts       one fun fact per country (answer sheet "Did you know?")
src/lib/engine.ts       question generation. Kinds: mc, map, order, intro, match.
                        generateFocusLesson (3-4 country sets, Duolingo-style),
                        generateDrill (map-heavy unit reps), generateSession
                        (exams/daily/sprint/rankings + scheduledSession for
                        learn/sandbox practice)
src/lib/srs.ts          spaced repetition (Leitner boxes, intervals 1/2/4/9/21/60d)
src/lib/paths.ts        continent path: units of ~7 → learn(setA)/learn(setB)/
                        drill/checkpoint nodes
src/lib/store.tsx       ProgressProvider: XP/hearts/streak+freezes/gems/mastery/
                        srs/reviewQueue/medals/challenges/pathProgress/prefs
src/lib/medals.ts       conquer all 4 challenges per continent → medal; defend
                        every 14 days via ultra quiz or it tarnishes
src/app/play/page.tsx   THE play loop, all modes: daily|learn|sprint|review|
                        sandbox|rankings|path|challenge|defend. Contains
                        MatchQuestion, IntroCard, CountrySpotlight (answer sheet),
                        quit dialog, in-session retry (once per FACT — see below)
src/components/WorldMap.tsx  zoomable map (pinch/wheel/double-tap/pan)
```

## Learning system (deliberate design — don't regress)

- Lessons = focus sets: meet 3-4 countries → match pairs → easy MC each →
  match reversed → hard recall each. Every fact ≥4 touches per lesson.
- Misses recycle at end of session **once per fact key `skill:countryId`**
  (keying by question id caused an infinite retry loop — fixed bug).
- SRS runs invisibly: recordAnswer schedules each fact; due facts fuel lesson
  warm-ups, Review mode, and home's review count.
- Daily challenge: seeded `daily-<date>`, identical for everyone (leaderboard-
  ready), heart-free, mistakes only cost score. Streak +1 on first completion
  per local day; freezes earned per 7 perfect dailies.
- Hearts gate learn/review/path only. Entering with 0 hearts shows a friendly
  gate (never insta-fail); only a WRONG answer may end a run at 0 hearts.
- Match exercises never cost hearts; they record per-pair answers.

## Tests

- `npm test` (vitest) covers the **pure logic only**: engine/srs/paths/medals/
  format. No DOM/component tests — the UI is verified by playing it. CI runs
  `npm test` before the build, so a red test blocks the deploy.
- Tests live next to their source as `src/lib/*.test.ts`. tsconfig has no
  `target` (so ES5): don't spread a Set in tests, use `Array.from`.
- The daily challenge's "same questions for everyone" promise IS `hashSeed` +
  `mulberry32` being deterministic — those are pinned hard. Don't touch them
  without expecting to break every player's shared daily.

## Open question — Oceania's shape challenge is all-or-nothing

Oceania has 7 countries; `fj` is `noShape` and `sb`/`vu`/`ws` are `tiny`, so
`challengePool("Oceania","shape")` is just au/nz/pg. Challenge count is
`min(pool.length, 45)` = 3, and `CHALLENGE_PASS` is 90% → 2/3 (66.7%) fails, so
only a perfect 3/3 conquers it. Every other continent tolerates a miss. Nobody
chose this; it fell out of the data. Pinned by a test in `medals.test.ts` so any
fix is deliberate and visible. Options if it should change: lower the pass for
tiny pools, pad the pool with `tiny` countries for shape, or floor the question
count. **Magnus's call.**

## Known engineering gotchas

- Next.js reuses the /play component across lessons → per-run state resets in
  the session-build effect (don't remove it; it fixed the stuck-completion bug).
- `advance(fromIdx)` has an idempotency guard (advancedFrom ref) against
  double-taps/stale timers. Sprint timer is wall-clock (bg-tab throttling).
- Framer transform animations override CSS transform centering — center modals
  with flex wrappers, never `-translate-x-1/2` on a motion.div.
- Metadata icon/manifest fields in layout must be manually base-path-prefixed;
  Next's `manifest` metadata field strips the prefix (use manual <link>).
- URL params (duel `from`/`target`) are clamped — keep treating params as untrusted.

## Roadmap (agreed with Magnus)

1. **Supabase backend** (top priority, waiting on him creating the free account):
   accounts, cross-device sync, global daily leaderboard, real friends. OpenAI
   API integration for personalized explanations also waits for this (never put
   API keys in this static site).
2. Type-the-answer questions; expand from 146 to all ~195 countries; sound toggle;
   streak-danger nudge.
3. Localization: English/Norwegian/Spanish (structure for more later).
4. Repo rename to `pangea`; Play Store via TWA; analytics (GoatCounter) and
   Sentry, both free tier.
